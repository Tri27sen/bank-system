
version: '3.8'

services:
  # Main application service
  bank-api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql://AutoBox_owner:SUhkErJ4KH0u@ep-tight-art-a50hk2av-pooler.us-east-2.aws.neon.tech/bank?sslmode=require&channel_binding=require
      - PYTHONPATH=/app
    volumes:
      # Mount source code for development (remove in production)
      - .:/app
    restart: unless-stopped
    networks:
      - bank-network

  # Optional: Local PostgreSQL for development/testing
  # Uncomment and configure if you want to use a local database
  # postgres:
  #   image: postgres:15-alpine
  #   environment:
  #     POSTGRES_DB: bank
  #     POSTGRES_USER: bankuser
  #     POSTGRES_PASSWORD: bankpass
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #     - ./init.sql:/docker-entrypoint-initdb.d/init.sql
  #   ports:
  #     - "5432:5432"
  #   restart: unless-stopped
  #   networks:
  #     - bank-network

networks:
  bank-network:
    driver: bridge

# volumes:
#   postgres_data: